"""Python List"""

list1 = list(map(int,input().split()))

"""
1. using append() and insert() Method : append() method only works for addition of the elements at the end of the list, 
for addition of an element at desired position, insert() method is used. 
Unlike append() which takes only one argument and insert() method takes 2 arguments(position, value).

2. extend() method is used to add multiple elements at the same time at the end of your list.

3. remove() method will remove one element at a time, to remove a range of elements, iterator is used.The remove() method
removes the specified items. Remove method will remove only the first occurrence of an element.

4. pop() function can be used to remove or return an element from the list, but by default it will remove only the 
last element of the list, to remove element from a specific position of the list, index of the element
is passed as an argument to the pop() method.







"""
